#include "main.h"
/**
 * main - entry point
 * @argc: argument count
 * @argv: argument vector
 * @env: an array of environment variables
 * Return: 0
 */
int main(int argc, char *argv[], char *env[])
{

(void)argc;
(void)argv;
prompt(env);
return (0);
}
